SELECT 
cst_id,
count(*) AS total_count
FROM silver.crm_cust_info
GROUp BY cst_id
having COUNT(*) > 1 AND cst_id IS NOT NULL

SELECT
cst_firstname
FROM silver.crm_cust_info
WHERE cst_firstname != TRIM(cst_firstname)

SELECT 
cst_id,
COUNT(*) AS total
FROM silver.crm_cust_info
GROUP BY cst_id
Having COUNT(*) >1 

SELECT 
DISTINCT cst_gndr
FROM  silver.crm_cust_info


SELECT *
FROM silver.crm_cust_info

-- Checked and the data is cleaned now--
